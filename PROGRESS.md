# Progress

Statuses: `todo` → `in-progress` → `done` (or `blocked` with a note).
The executing agent updates this file at the start and end of every task session.

> **Stack note (2026-07-18):** backend is **TypeScript/Node** (Hono + pg + Kysely), not Go — the Go/.NET toolchains are unavailable/blocked on this machine. See [ADR 0004](docs/adr/0004-typescript-node-backend.md). Repo is an npm-workspaces monorepo with `packages/shared` (HMAC + Zod). Schema, security, and product behavior are unchanged.

| # | Task | Status | Date | Notes |
|----|------|--------|------|-------|
| 01 | repo-scaffold | done | 2026-07-18 | Monorepo tree per idea.md §18 at repo root (no nested family-tree/). Added .gitignore, .editorconfig, .gitattributes (LF enforced; .sh stay LF for Linux), README. db/generated committed via .gitkeep. No package.json/go.mod/CI yet (later tasks). |
| 02 | architecture-docs-adrs | done | 2026-07-18 | docs/architecture.md (components, flow, network, auth, env inventory), docs/data-model.md (Mermaid erDiagram covering all 19 tables incl. nonces + idempotency_keys, per-table notes, enum summary), docs/security.md (threat model, HMAC, rate limits, GDPR). ADR 0001 (HMAC BFF), 0002 (staging vs canonical), 0003 (relational graph). Mermaid validated structurally via Node (depth 0, all required tables present); no browser render tool run. |
| 03 | openapi-contract | done | 2026-07-18 | contracts/openapi.yaml — OpenAPI 3.1 covering all §16 endpoints + invites (create/list/revoke/validate), tags per area, HMAC documented via ServiceHmac scheme + reusable header params, idempotency + uniform Error, tree projection & relationship-path examples verbatim from idea.md §13/§11. redocly lint: valid, 0 errors, 4 advisory warnings (SourceType reserved for task-24, etc). operationIds added. |
| 04 | api-skeleton-dev-env | done | 2026-07-19 | npm workspaces root (TS6/ESLint10/Vitest4) + @familytree/shared stub + @familytree/api (Hono 4, pg, Kysely 0.29, pino, zod 4). Health/ready, request-id, uniform errors, graceful shutdown. Static migration registry (db/migrations/index.ts) so bundled dist/db/migrate.js works in prod — verified. kysely-codegen wired (0 tables). Dev postgres compose (127.0.0.1:5433, UTF8, +familytree_test). 12 unit + 1 integration tests green; bundled server smoke-tested (/health, /ready 200). Note: Kysely 0.29 moved Migrator to 'kysely/migration' subpath. |
| 05 | migrations-staging-layer | done | 2026-07-19 | Migration 0001_staging_tables: invites, submissions, submission_people, submission_relationships, consents, audit_log, service_request_nonces, idempotency_keys — all CHECK/UNIQUE constraints per data-model, staging children CASCADE from submissions. Typed repos: invites (guarded atomic increment), submissions, audit, service-auth (nonce/idempotency with ON CONFLICT semantics). kysely-codegen regenerated (kysely_* excluded). 12 new integration tests: up/down/up cycle, dup local_key, used>max, guarded increment (max/revoked/expired), status CHECK, year-range CHECK, dup relationship, nonce replay, idempotency roundtrip+expiry. Bundled dist/db/migrate.js sees the migration. |
| 06 | migrations-canonical-layer | done | 2026-07-19 | Migration 0002_canonical_tables: people (+self-merge CHECK), person_names (partial unique preferred per type), places (UNIQUE NULLS NOT DISTINCT dedupe), person_events (year+date CHECKs), parent_child_relationships (self-parent CHECK, UNIQUE(parent,child,type), confidence 0-100), family_unions+union_partners, sources, evidence (ON DELETE RESTRICT), match_candidates (unique pair), person_merge_history. Deferred FK submission_people.matched_person_id → people added. Repos: people (+names/events/merge history, defaultValues fix for all-default inserts), places (concurrent-safe getOrCreate), genealogy (edges+unions), sources/evidence, matching (upsert preserves review state). 14 new integration tests — full acceptance matrix green (27 integration total). reviewed_by is TEXT (admin email) — ER updated. Bundled migrate sees both migrations. |
| 07 | hmac-auth-middleware | done | 2026-07-19 | Shared signer/verifier in packages/shared (sign+verify same module — no drift), contracts/hmac.md spec, 6 golden vectors + generator script. Hono middleware on /v1/internal/*: crypto checks first (headers/skew/body-hash/timingSafeEqual), then nonce store + idempotency (claim/replay with Idempotent-Replay header/409 conflict). requireRole('admin'). Hourly janitor (unref'd, stopped on shutdown). SERVICE_ID/SERVICE_HMAC_SECRET required in config. Decision: idempotency request_hash excludes nonce+timestamp (retries rotate nonces — hashing full canonical would break replay); check order puts pure crypto before DB writes (DoS hygiene). 20 shared + 10 middleware unit tests, 4 integration; prod smoke: unsigned internal → 401, /health public. |
| 08 | nextjs-skeleton | done | 2026-07-19 | Next.js 16.2 (App Router, RSC) + React 19.2 + Tailwind v4 (CSS-first) + shadcn/ui (new-york, neutral, 14 primitives: button/input/label/textarea/checkbox/radio-group/select/card/dialog/table/badge/tabs/sonner/form) in apps/web as @familytree/web workspace. lang="bg", Inter+JetBrains_Mono with cyrillic subset (self-hosted next/font), BG metadata/header/footer/nav, home with CTAs, BG error.tsx+not-found.tsx, /questionnaire+/tree placeholders, TanStack Query provider, sonner Toaster. transpilePackages @familytree/shared. Vitest+jsdom+Testing Library smoke test. .env.example (only NEXT_PUBLIC_TURNSTILE_SITE_KEY public). Root eslint ignores apps/web (own Next config). Verified: typecheck, lint, 2 tests, production build (static). |
| 09 | bff-signing-client | done | 2026-07-19 | Server-only BFF lib in apps/web/src/server: env.ts (Zod server env, cached, server-only guard), oracle/sign.ts (thin wrapper over @familytree/shared signRequest — no reimpl), oracle/client.ts (oracleFetch: single body serialization + X-Body-SHA256, all HMAC headers, X-Request-Id correlation, 10s AbortController timeout, GET-only retry, no secret logging), oracle/errors.ts (OracleError normalization, internal details never pass through), fingerprint.ts (clientFingerprint HMAC, IP normalization: port/zone/mapped-IPv6, extractClientIp x-forwarded-for first hop), idempotency.ts. server-only aliased to a stub in vitest. 31 web tests incl. 6 golden-vector parity (BFF wrapper reproduces identical signatures), fingerprint normalization, error normalization, oracleFetch header/hash/retry/timeout. typecheck+lint+build clean. |
| 10 | docker-caddy-infra | done | 2026-07-19 | services/api/Dockerfile: multi-stage node:22-bookworm-slim, scoped npm ci (--workspace @familytree/api, skips web toolchain), esbuild bundle, runtime = node user + dist only, Node fetch HEALTHCHECK, arm64. .dockerignore. infra/oracle/docker-compose.prod.yml: caddy(80/443 only)/api/postgres, public+internal(internal:true) nets, no db/api host ports, app non-superuser role via initdb/01-app-user.sh, healthchecks, one-shot migrate service (profile ops), json-file log rotation, read_only api+tmpfs+no-new-privileges. Caddyfile (HSTS/nosniff/DENY/CSP default-src none, -Server, 1MB body, admin off) — caddy validate OK. cloud-init.yaml (deploy user, docker, UFW 22-from-admin/80/443, ssh hardening). env.example (BG comments), firewall.md, docs/deployment-oracle-bg.md. VERIFIED: compose config valid + only 80/443 published, caddy validate, ARM64 buildx build + emulated run serving /health+/ready 200 (arch=arm64). |
| 11 | ci-pipeline | done | 2026-07-19 | .github/workflows/ci.yml — jobs: web (shared+web lint/typecheck/test/build), api (postgres:16 service, create familytree_test, lint/typecheck/unit, migrate:up + codegen drift check via git diff --exit-code, integration, build), docker-arm64 (buildx linux/arm64, no push), contracts (redocly lint), gitleaks (full-history scan). Single root npm ci with setup-node cache, concurrency cancel-in-progress, read perms. .gitleaks.toml allowlists test fixtures + *.example placeholders. VERIFIED LOCALLY: actionlint clean, codegen drift clean, redocly clean, gitleaks 0 leaks with allowlist. |
| 12 | invitations | done | 2026-07-19 | Invites service+routes: POST/GET/revoke (admin via requireRole), GET validate (service-level, no admin). Token = inv_ + 32B base64url; only sha256 hash stored; plain token returned once. consumeInvite(trx, token) atomic guarded-by-hash increment for Task 16 (concurrent race → exactly one success, verified). validateToken with precise reasons (expired/revoked/exhausted/not_found; not_found not distinguished from bad format). Audit invite.created/revoked. Established the ROUTING PATTERN: createApp now takes db + mounts registerInviteRoutes under /v1/internal (hmac already applied); transport/validate.ts parseJson (uniform 400). OpenAPI unchanged (task-03 already had these). 3 unit + 6 integration (create/hash-only/list-no-token/validate-reasons/idempotent-revoke/403-non-admin/concurrent-consume). CI on main verified green (PR #8). |
| 13 | questionnaire-schema-validation | done | 2026-07-19 | apps/web/src/features/questionnaire: local-keys.ts (fixed keys + SIBLING/CHILD/PARTNER/RELATIVE_n factories + parse/validate), labels.ts (BG enum labels: living/parent-rel/contact/materials/consents + step titles, CONSENT_VERSION 2026-07-19), schema.ts (Zod per step; plainText no-HTML + max-len, year 1800..now + from<=to, honeypot website must be empty, formStartedAt; minimal-path: steps 3-6 validate empty; materials Q in step6; 5 consents step7, only data_processing required; no exact dates collected → living never get precise DOB), payload.ts (deterministic toSubmissionPayload: SELF+parents+grandparents+siblings/children/partners/relatives, relationship derivation SELF↔parents/parents↔grandparents/siblings/children/partners/other, empty blocks skipped, edges only when both present), draft.ts (localStorage versioned, lenient parse, corrupt→discard). 16 tests (minimal path, required consent, honeypot, HTML/length, year range, cap 10, payload assembly+derivation, draft roundtrip+corrupt). 47 web tests green; build clean. |
| 14 | questionnaire-ui-steps-1-4 | done | 2026-07-19 | Multi-step shell: QuestionnaireProvider (React context — chosen over Zustand) holds accumulated values + step + draft; commitStep validates+merges+advances, back preserves, debounced autosave (800ms), draftSavedAt indicator, restore banner gates the form so step forms mount with correct defaults. RHF+zodResolver per step. Stepper (compact "Стъпка N от M" + bar on mobile, numbered on desktop). Mobile-first: single-column, font≥16px inputs (text-base), sticky bottom StepNav. Honeypot (aria-hidden, tabIndex -1, off-screen) + formStartedAt. Reusable PersonBlockFields (fieldset/legend, nested-error reader, toggleable death/occupation/relationshipType/familyStories/infoSource). Steps 1-4 (participant+fillingForOther+required consent, self, parents cards, 4 grandparents); steps 3-4 "Пропусни тази стъпка" clears+advances. Steps 5-7 placeholders (task-15). Fixed React Compiler lint (Next 16): Date.now via useState lazy init, ref-in-cleanup rewrite, targeted set-state-in-effect disable for client-only draft detection. 6 UI tests (render+honeypot hidden, block-on-invalid, advance, skip, draft restore/discard); 53 web tests green; build clean. |
| 15 | questionnaire-ui-steps-5-7 | done | 2026-07-20 | Step5 (useFieldArray: siblings/children/partners/unclesAunts/otherRelatives add-remove cards + contactPerson, cap 10 with BG notice, skippable). Step6 (origin fields + materials radio yes/no/unsure + description, skippable). Step7 (read-only summary grouped by step with Редактирай→goTo, BG nudges for empty parents/grandparents/origin, 5 consent checkboxes only data_processing required, Turnstile widget @marsidev/react-turnstile with dev-fallback when no site key, submit gated on consent+token). submit.ts: assemble→questionnaireSchema validate→toSubmissionPayload→POST /api/questionnaire/submit; sessionStorage idempotency key reused across retries + cleared on success; maps 429→rate_limited, 400 turnstile_failed→turnstile, validation, network→error. success page + SuccessContent (reference code + snowball share via Web Share API/clipboard). Provider gained initialValues. StepNav submitDisabled. 12 tests (submit: payload shape/idempotency reuse/429/turnstile/validation/network; steps: fieldarray add-remove, cap 10, nudge, consent gating, success redirect, 429 message). 65 web tests green; lint+build clean. Route /api/questionnaire/submit lands in task-16 (tests mock fetch). |
| 16 | submission-pipeline | done | 2026-07-20 | END-TO-END (DoD 1-4 met). API: createSubmission (single tx: invite consume OR fingerprint rate-limit 3/24h, insert submission pending|spam, explode payload→submission_people [year approx→±3 window, placeholder normalize] + relationships + consents, audit); read.ts (admin list with participantName/campaign/peopleCount/hasMaterials + detail with fingerprint PREFIX only); routes POST /submissions (public actor) + GET list/detail (requireRole admin). BFF: submit route (100KB size guard→413, Turnstile server-verify→400, honeypot/durationMs<60s→spamSignal silently, HMAC fingerprint never raw IP, idempotency key, oracleFetch public actor, 429 passthrough, no internal leak→502), invite-check route, turnstile.ts (dev-skip when no secret). Insert dates use new Date() not raw sql (Kysely Insertable typing). Test isolation: unique fingerprints + delta counts (shared familytree_test accumulates). 7 API integration (pending+staging+canonical-untouched, rate-limit, honeypot→spam, idempotent replay 1 row, invite consume+exhaust, admin list/detail prefix, 403 public) + 8 BFF unit (413, turnstile, fingerprint-no-raw-IP, honeypot, too_fast, 201, 429, no-leak). 27 API unit + 44 integration + 73 web green; builds clean. |
| 17 | admin-auth | done | 2026-07-20 | Auth.js v5 (next-auth 5.0.0-beta.31) + Google provider, JWT sessions 8h/updateAge 1h. admin-allowlist.ts (case-insensitive CSV, edge-safe) → signIn callback rejects non-allowlisted; jwt/session stamp role='admin'. authorized callback (in proxy) gates /admin (redirect to login) + /api/admin (401 JSON, not redirect). src/proxy.ts (Next 16 renamed middleware→proxy; export auth as proxy, matcher /admin+/api/admin). requireAdminSession(req) → {ok, actor.email} | 401, plus X-Admin-Request marker required on mutations (CSRF defense). next-auth.d.ts augments Session.user.role. /admin/login (Google server-action signIn + error), /admin layout shell (sidebar Заявки/Покани/Хора/Дърво, email + Изход signOut, renders plain for unauthenticated=login), /admin landing, /api/admin/ping test route. 9 unit tests (allowlist matrix, requireAdminSession session/role/marker/GET). SMOKE VERIFIED: /api/admin/ping → 401 BG JSON without session, / → 200. Decision: single Google provider (GitHub deferrable). 82 web tests green; build clean (no deprecation). |
| 18 | admin-submissions-inbox | todo | | |
| 19 | name-normalization | todo | | |
| 20 | match-engine | todo | | |
| 21 | canonical-people-crud | todo | | |
| 22 | parent-child-cycle-prevention | todo | | |
| 23 | family-unions | todo | | |
| 24 | sources-evidence | todo | | |
| 25 | person-merge | todo | | |
| 26 | admin-people-browser | todo | | |
| 27 | admin-review-workspace | todo | | |
| 28 | genealogy-queries-resolver | todo | | |
| 29 | tree-projection-api | todo | | |
| 30 | privacy-redaction | todo | | |
| 31 | tree-visualization | todo | | |
| 32 | tree-ux-path-highlight | todo | | |
| 33 | backup-restore | todo | | |
| 34 | exports-gedcom-json-csv | todo | | |
| 35 | deploy-workflows | todo | | |
| 36 | monitoring | todo | | |
| 37 | e2e-playwright | todo | | |
| 38 | file-uploads (post-MVP, optional) | todo | | |
