# Progress

Statuses: `todo` → `in-progress` → `done` (or `blocked` with a note).
The executing agent updates this file at the start and end of every task session.

> **Stack note (2026-07-18):** backend is **TypeScript/Node** (Hono + pg + Kysely), not Go — the Go/.NET toolchains are unavailable/blocked on this machine. See [ADR 0004](docs/adr/0004-typescript-node-backend.md). Repo is an npm-workspaces monorepo with `packages/shared` (HMAC + Zod). Schema, security, and product behavior are unchanged.

| # | Task | Status | Date | Notes |
|----|------|--------|------|-------|
| 01 | repo-scaffold | done | 2026-07-18 | Monorepo tree per idea.md §18 at repo root (no nested family-tree/). Added .gitignore, .editorconfig, .gitattributes (LF enforced; .sh stay LF for Linux), README. db/generated committed via .gitkeep. No package.json/go.mod/CI yet (later tasks). |
| 02 | architecture-docs-adrs | done | 2026-07-18 | docs/architecture.md (components, flow, network, auth, env inventory), docs/data-model.md (Mermaid erDiagram covering all 19 tables incl. nonces + idempotency_keys, per-table notes, enum summary), docs/security.md (threat model, HMAC, rate limits, GDPR). ADR 0001 (HMAC BFF), 0002 (staging vs canonical), 0003 (relational graph). Mermaid validated structurally via Node (depth 0, all required tables present); no browser render tool run. |
| 03 | openapi-contract | done | 2026-07-18 | contracts/openapi.yaml — OpenAPI 3.1 covering all §16 endpoints + invites (create/list/revoke/validate), tags per area, HMAC documented via ServiceHmac scheme + reusable header params, idempotency + uniform Error, tree projection & relationship-path examples verbatim from idea.md §13/§11. redocly lint: valid, 0 errors, 4 advisory warnings (SourceType reserved for task-24, etc). operationIds added. |
| 04 | api-skeleton-dev-env | done | 2026-07-19 | npm workspaces root (TS6/ESLint10/Vitest4) + @familytree/shared stub + @familytree/api (Hono 4, pg, Kysely 0.29, pino, zod 4). Health/ready, request-id, uniform errors, graceful shutdown. Static migration registry (db/migrations/index.ts) so bundled dist/db/migrate.js works in prod — verified. kysely-codegen wired (0 tables). Dev postgres compose (127.0.0.1:5433, UTF8, +familytree_test). 12 unit + 1 integration tests green; bundled server smoke-tested (/health, /ready 200). Note: Kysely 0.29 moved Migrator to 'kysely/migration' subpath. |
| 05 | migrations-staging-layer | done | 2026-07-19 | Migration 0001_staging_tables: invites, submissions, submission_people, submission_relationships, consents, audit_log, service_request_nonces, idempotency_keys — all CHECK/UNIQUE constraints per data-model, staging children CASCADE from submissions. Typed repos: invites (guarded atomic increment), submissions, audit, service-auth (nonce/idempotency with ON CONFLICT semantics). kysely-codegen regenerated (kysely_* excluded). 12 new integration tests: up/down/up cycle, dup local_key, used>max, guarded increment (max/revoked/expired), status CHECK, year-range CHECK, dup relationship, nonce replay, idempotency roundtrip+expiry. Bundled dist/db/migrate.js sees the migration. |
| 06 | migrations-canonical-layer | done | 2026-07-19 | Migration 0002_canonical_tables: people (+self-merge CHECK), person_names (partial unique preferred per type), places (UNIQUE NULLS NOT DISTINCT dedupe), person_events (year+date CHECKs), parent_child_relationships (self-parent CHECK, UNIQUE(parent,child,type), confidence 0-100), family_unions+union_partners, sources, evidence (ON DELETE RESTRICT), match_candidates (unique pair), person_merge_history. Deferred FK submission_people.matched_person_id → people added. Repos: people (+names/events/merge history, defaultValues fix for all-default inserts), places (concurrent-safe getOrCreate), genealogy (edges+unions), sources/evidence, matching (upsert preserves review state). 14 new integration tests — full acceptance matrix green (27 integration total). reviewed_by is TEXT (admin email) — ER updated. Bundled migrate sees both migrations. |
| 07 | hmac-auth-middleware | done | 2026-07-19 | Shared signer/verifier in packages/shared (sign+verify same module — no drift), contracts/hmac.md spec, 6 golden vectors + generator script. Hono middleware on /v1/internal/*: crypto checks first (headers/skew/body-hash/timingSafeEqual), then nonce store + idempotency (claim/replay with Idempotent-Replay header/409 conflict). requireRole('admin'). Hourly janitor (unref'd, stopped on shutdown). SERVICE_ID/SERVICE_HMAC_SECRET required in config. Decision: idempotency request_hash excludes nonce+timestamp (retries rotate nonces — hashing full canonical would break replay); check order puts pure crypto before DB writes (DoS hygiene). 20 shared + 10 middleware unit tests, 4 integration; prod smoke: unsigned internal → 401, /health public. |
| 08 | nextjs-skeleton | done | 2026-07-19 | Next.js 16.2 (App Router, RSC) + React 19.2 + Tailwind v4 (CSS-first) + shadcn/ui (new-york, neutral, 14 primitives: button/input/label/textarea/checkbox/radio-group/select/card/dialog/table/badge/tabs/sonner/form) in apps/web as @familytree/web workspace. lang="bg", Inter+JetBrains_Mono with cyrillic subset (self-hosted next/font), BG metadata/header/footer/nav, home with CTAs, BG error.tsx+not-found.tsx, /questionnaire+/tree placeholders, TanStack Query provider, sonner Toaster. transpilePackages @familytree/shared. Vitest+jsdom+Testing Library smoke test. .env.example (only NEXT_PUBLIC_TURNSTILE_SITE_KEY public). Root eslint ignores apps/web (own Next config). Verified: typecheck, lint, 2 tests, production build (static). |
| 09 | bff-signing-client | done | 2026-07-19 | Server-only BFF lib in apps/web/src/server: env.ts (Zod server env, cached, server-only guard), oracle/sign.ts (thin wrapper over @familytree/shared signRequest — no reimpl), oracle/client.ts (oracleFetch: single body serialization + X-Body-SHA256, all HMAC headers, X-Request-Id correlation, 10s AbortController timeout, GET-only retry, no secret logging), oracle/errors.ts (OracleError normalization, internal details never pass through), fingerprint.ts (clientFingerprint HMAC, IP normalization: port/zone/mapped-IPv6, extractClientIp x-forwarded-for first hop), idempotency.ts. server-only aliased to a stub in vitest. 31 web tests incl. 6 golden-vector parity (BFF wrapper reproduces identical signatures), fingerprint normalization, error normalization, oracleFetch header/hash/retry/timeout. typecheck+lint+build clean. |
| 10 | docker-caddy-infra | done | 2026-07-19 | services/api/Dockerfile: multi-stage node:22-bookworm-slim, scoped npm ci (--workspace @familytree/api, skips web toolchain), esbuild bundle, runtime = node user + dist only, Node fetch HEALTHCHECK, arm64. .dockerignore. infra/oracle/docker-compose.prod.yml: caddy(80/443 only)/api/postgres, public+internal(internal:true) nets, no db/api host ports, app non-superuser role via initdb/01-app-user.sh, healthchecks, one-shot migrate service (profile ops), json-file log rotation, read_only api+tmpfs+no-new-privileges. Caddyfile (HSTS/nosniff/DENY/CSP default-src none, -Server, 1MB body, admin off) — caddy validate OK. cloud-init.yaml (deploy user, docker, UFW 22-from-admin/80/443, ssh hardening). env.example (BG comments), firewall.md, docs/deployment-oracle-bg.md. VERIFIED: compose config valid + only 80/443 published, caddy validate, ARM64 buildx build + emulated run serving /health+/ready 200 (arch=arm64). |
| 11 | ci-pipeline | done | 2026-07-19 | .github/workflows/ci.yml — jobs: web (shared+web lint/typecheck/test/build), api (postgres:16 service, create familytree_test, lint/typecheck/unit, migrate:up + codegen drift check via git diff --exit-code, integration, build), docker-arm64 (buildx linux/arm64, no push), contracts (redocly lint), gitleaks (full-history scan). Single root npm ci with setup-node cache, concurrency cancel-in-progress, read perms. .gitleaks.toml allowlists test fixtures + *.example placeholders. VERIFIED LOCALLY: actionlint clean, codegen drift clean, redocly clean, gitleaks 0 leaks with allowlist. |
| 12 | invitations | done | 2026-07-19 | Invites service+routes: POST/GET/revoke (admin via requireRole), GET validate (service-level, no admin). Token = inv_ + 32B base64url; only sha256 hash stored; plain token returned once. consumeInvite(trx, token) atomic guarded-by-hash increment for Task 16 (concurrent race → exactly one success, verified). validateToken with precise reasons (expired/revoked/exhausted/not_found; not_found not distinguished from bad format). Audit invite.created/revoked. Established the ROUTING PATTERN: createApp now takes db + mounts registerInviteRoutes under /v1/internal (hmac already applied); transport/validate.ts parseJson (uniform 400). OpenAPI unchanged (task-03 already had these). 3 unit + 6 integration (create/hash-only/list-no-token/validate-reasons/idempotent-revoke/403-non-admin/concurrent-consume). CI on main verified green (PR #8). |
| 13 | questionnaire-schema-validation | done | 2026-07-19 | apps/web/src/features/questionnaire: local-keys.ts (fixed keys + SIBLING/CHILD/PARTNER/RELATIVE_n factories + parse/validate), labels.ts (BG enum labels: living/parent-rel/contact/materials/consents + step titles, CONSENT_VERSION 2026-07-19), schema.ts (Zod per step; plainText no-HTML + max-len, year 1800..now + from<=to, honeypot website must be empty, formStartedAt; minimal-path: steps 3-6 validate empty; materials Q in step6; 5 consents step7, only data_processing required; no exact dates collected → living never get precise DOB), payload.ts (deterministic toSubmissionPayload: SELF+parents+grandparents+siblings/children/partners/relatives, relationship derivation SELF↔parents/parents↔grandparents/siblings/children/partners/other, empty blocks skipped, edges only when both present), draft.ts (localStorage versioned, lenient parse, corrupt→discard). 16 tests (minimal path, required consent, honeypot, HTML/length, year range, cap 10, payload assembly+derivation, draft roundtrip+corrupt). 47 web tests green; build clean. |
| 14 | questionnaire-ui-steps-1-4 | done | 2026-07-19 | Multi-step shell: QuestionnaireProvider (React context — chosen over Zustand) holds accumulated values + step + draft; commitStep validates+merges+advances, back preserves, debounced autosave (800ms), draftSavedAt indicator, restore banner gates the form so step forms mount with correct defaults. RHF+zodResolver per step. Stepper (compact "Стъпка N от M" + bar on mobile, numbered on desktop). Mobile-first: single-column, font≥16px inputs (text-base), sticky bottom StepNav. Honeypot (aria-hidden, tabIndex -1, off-screen) + formStartedAt. Reusable PersonBlockFields (fieldset/legend, nested-error reader, toggleable death/occupation/relationshipType/familyStories/infoSource). Steps 1-4 (participant+fillingForOther+required consent, self, parents cards, 4 grandparents); steps 3-4 "Пропусни тази стъпка" clears+advances. Steps 5-7 placeholders (task-15). Fixed React Compiler lint (Next 16): Date.now via useState lazy init, ref-in-cleanup rewrite, targeted set-state-in-effect disable for client-only draft detection. 6 UI tests (render+honeypot hidden, block-on-invalid, advance, skip, draft restore/discard); 53 web tests green; build clean. |
| 15 | questionnaire-ui-steps-5-7 | done | 2026-07-20 | Step5 (useFieldArray: siblings/children/partners/unclesAunts/otherRelatives add-remove cards + contactPerson, cap 10 with BG notice, skippable). Step6 (origin fields + materials radio yes/no/unsure + description, skippable). Step7 (read-only summary grouped by step with Редактирай→goTo, BG nudges for empty parents/grandparents/origin, 5 consent checkboxes only data_processing required, Turnstile widget @marsidev/react-turnstile with dev-fallback when no site key, submit gated on consent+token). submit.ts: assemble→questionnaireSchema validate→toSubmissionPayload→POST /api/questionnaire/submit; sessionStorage idempotency key reused across retries + cleared on success; maps 429→rate_limited, 400 turnstile_failed→turnstile, validation, network→error. success page + SuccessContent (reference code + snowball share via Web Share API/clipboard). Provider gained initialValues. StepNav submitDisabled. 12 tests (submit: payload shape/idempotency reuse/429/turnstile/validation/network; steps: fieldarray add-remove, cap 10, nudge, consent gating, success redirect, 429 message). 65 web tests green; lint+build clean. Route /api/questionnaire/submit lands in task-16 (tests mock fetch). |
| 16 | submission-pipeline | done | 2026-07-20 | END-TO-END (DoD 1-4 met). API: createSubmission (single tx: invite consume OR fingerprint rate-limit 3/24h, insert submission pending|spam, explode payload→submission_people [year approx→±3 window, placeholder normalize] + relationships + consents, audit); read.ts (admin list with participantName/campaign/peopleCount/hasMaterials + detail with fingerprint PREFIX only); routes POST /submissions (public actor) + GET list/detail (requireRole admin). BFF: submit route (100KB size guard→413, Turnstile server-verify→400, honeypot/durationMs<60s→spamSignal silently, HMAC fingerprint never raw IP, idempotency key, oracleFetch public actor, 429 passthrough, no internal leak→502), invite-check route, turnstile.ts (dev-skip when no secret). Insert dates use new Date() not raw sql (Kysely Insertable typing). Test isolation: unique fingerprints + delta counts (shared familytree_test accumulates). 7 API integration (pending+staging+canonical-untouched, rate-limit, honeypot→spam, idempotent replay 1 row, invite consume+exhaust, admin list/detail prefix, 403 public) + 8 BFF unit (413, turnstile, fingerprint-no-raw-IP, honeypot, too_fast, 201, 429, no-leak). 27 API unit + 44 integration + 73 web green; builds clean. |
| 17 | admin-auth | done | 2026-07-20 | Auth.js v5 (next-auth 5.0.0-beta.31) + Google provider, JWT sessions 8h/updateAge 1h. admin-allowlist.ts (case-insensitive CSV, edge-safe) → signIn callback rejects non-allowlisted; jwt/session stamp role='admin'. authorized callback (in proxy) gates /admin (redirect to login) + /api/admin (401 JSON, not redirect). src/proxy.ts (Next 16 renamed middleware→proxy; export auth as proxy, matcher /admin+/api/admin). requireAdminSession(req) → {ok, actor.email} | 401, plus X-Admin-Request marker required on mutations (CSRF defense). next-auth.d.ts augments Session.user.role. /admin/login (Google server-action signIn + error), /admin layout shell (sidebar Заявки/Покани/Хора/Дърво, email + Изход signOut, renders plain for unauthenticated=login), /admin landing, /api/admin/ping test route. 9 unit tests (allowlist matrix, requireAdminSession session/role/marker/GET). SMOKE VERIFIED: /api/admin/ping → 401 BG JSON without session, / → 200. Decision: single Google provider (GitHub deferrable). 82 web tests green; build clean (no deprecation). |
| 18 | admin-submissions-inbox | done | 2026-07-20 | Stage 2 COMPLETE. API: workflow.ts (start-review pending→in_review, reject/mark-spam pending|in_review→rejected/spam; state machine → 409 invalid_transition; sets timestamps; audit) + contact-leads.ts (JSONB aggregation: consented participants + referral people with infoSource, non-spam only, no new tables); routes wired. BFF: admin-proxy.ts (requireAdminSession + X-Admin-Request CSRF + oracleFetch admin actor + error normalization), 8 thin route handlers (submissions list/detail/start-review/reject/mark-spam, invites GET/POST/revoke, contact-leads). Admin UI: /admin/submissions (TanStack Query, status tabs, table + Материали badge), /admin/submissions/[id] (payload sections, people/relationships/consents, materials note, raw-JSON toggle, start-review + reject/spam reason dialogs, optimistic invalidate + toast), /admin/invites (create form, token-shown-once dialog with share link, list + revoke, Потенциални контакти panel with prefill). client.ts sets X-Admin-Request. 6 API integration (transitions legal/illegal 409, reason required, audit, 404, leads aggregation, 403 public) + 2 web (list badges, detail sections). OpenAPI contact-leads added. 27 API unit + 50 integration + 84 web green; builds clean. |
| 19 | name-normalization | done | 2026-07-20 | src/names: normalize (NFC→lowercase→trim→collapse ws→strip punct except hyphen→ѝ→и, idempotent, \p{L}\p{N} unicode-aware), transliterate (2009 streamlined table + -ия→ia ending rule: Живков→zhivkov/Църков→tsarkov/Щерев→shterev/Юлия→yulia/София→sofia), surnameVariants (gender pairs ски↔ска/ов↔ова/ев↔ева + transliterations; Latin fold sky→ski/y→i so Митовски/Митовска/Mitovski/Mitovsky share "mitovski"; unrelated surnames stay separate), searchTokens (normalized+translit, dedup, sorted), buildPersonNameRow for Task 21. Wired into submission ingest (replaced placeholder → normalize()). 11 unit tests (incl. NFC=NFD, idempotency property, variant overlap, no false-merge) + integration assertion (SELF stored as 'иван митовски'). 38 API unit + 50 integration green; build clean. |
| 20 | match-engine | done | 2026-07-20 | score.ts pure Score(sp,cp) with documented weights (name 35/partial, surname 10, year exact15/±1 10/±3 5, birthplace 10, parents 15, partner 5, children 5, nickname 5, cap 100) + BG reasons. service.findMatches: prefilter by shared name token OR birth year ±2 (exclude merged/deleted, limit 50), score in TS, keep ≥30 top 10, UPSERT match_candidates (refresh score/reasons, PRESERVE reviewed status/reviewed_by), matching.run audit, never sets matched_person_id/creates people. POST find-matches (admin). 7 unit + 4 integration (rank+reasons+no-autolink, upsert preserves accepted no-dup, 404, 403). 45 API unit + 54 integration green. OpenAPI already had endpoint (task-03). |
| 21 | canonical-people-crud | done | 2026-07-20 | people/service+aggregate+routes. POST /people (manual create + preferred primary name), GET /people (variant-aware search), GET /people/:id (aggregate: names/events-with-places/parents/children/unions/sourceCount; merged→409 redirect envelope, deleted→404), PATCH /people/:id (privacy/living/notes). upsertPlaceByText (concurrent-safe dedupe). create-person promotion (tx, guards: submission in_review + sp pending/deferred→422; person private default; primary+birth(if differs)+nickname names normalized/transliterated; HONEST-PRECISION events: year_from==to→year, else→range, single→approximate, NEVER fake date; residence; questionnaire source idempotent + supports evidence on name+birth; sets staging created+matched_person_id). link-person (target not merged/deleted→422; alias if new name; identity evidence; match_candidate→accepted; staging linked). sourceCount counts distinct sources across person+names+events. 8 integration (create builds all + honest precision, approximate→range, link+alias+candidate-accept, place dedupe, in-review guard 422, merged-target 422, merged GET 409, 403 public). 45 API unit + 62 integration green; build clean. |
| 22 | parent-child-cycle-prevention | done | 2026-07-20 | cycle.ts: wouldCreateCycle (recursive CTE ancestors-of(parent) over proposed+confirmed edges → true if child appears or parent==child) + lockPersonPair (pg_advisory_xact_lock(hashtext(lo),hashtext(hi)) two-int4 overload, sorted pair). relationships-service: createParentChildEdge (tx, advisory lock, active-person guards, dup(parent,child,type)→409, cycle check for proposed/confirmed→422 cycle_detected, audit), patchParentChildEdge (transition matrix proposed→confirmed/disputed/rejected etc→409 else, confirm re-runs cycle check in tx), deleteParentChildEdge (snapshot to audit), edgesBetween. Routes: POST parent-child, PATCH/DELETE :id, GET between. FIXED: advisory lock needed hashtext (int4) not hashtextextended (int8→no signature→500). 9 integration = full §12 matrix (self-parent, direct cycle, multi-gen cycle, dup edge, adoption alongside biological, disputed doesn't block, concurrent A→B/B→A exactly one wins, proposed also blocks, 403 public). 45 API unit + 71 integration green; build clean. data-model already documents proposed+confirmed cycle scope. |
| 23 | family-unions | done | 2026-07-20 | unions-service: createUnion (1-2 distinct active partners, else 422), getUnionView (partners + children via family_union_id), patchUnion (unionType), addPartner (max 2, dup→409), removePartner, deleteUnion (blocked 409 union_in_use when a child edge references it). Routes: POST /unions, GET/PATCH/DELETE /:id, POST /:id/partners, DELETE /:id/partners/:personId. Person may belong to many unions (no spouse_id; aggregate already lists unions with partnerIds). listParentChildByUnion ready for Task 29. 5 integration (two sequential unions on aggregate, dup partner + max-2 → 409, delete blocked when child-edge references + unused deletes 204, GET partners+children, 403 public). 45 API unit + 76 integration green; build clean. |
| 24 | sources-evidence | done | 2026-07-20 | sources/service+routes: source CRUD (create/list by type+q/get-with-evidence/patch/delete→409 source_in_use when evidence exists), evidence create (validates subject row exists for subject_type via SUBJECT_TABLE map→422 if missing)/list-by-subject/delete. INVARIANT enforced+tested: evidence never mutates subject (disputes stance leaves person byte-identical). Questionnaire-source helper (ensureQuestionnaireSource, task-21) already idempotent. Person aggregate sourceCount already spans person+names+events. 6 integration (multi-source mixed stance, disputes→subject byte-identical, delete blocked 409 + unused 204, unknown subject 422, aggregate sourceCount, 403 public). 45 API unit + 82 integration green; build clean. |
| 25 | person-merge | done | 2026-07-20 | people/merge.ts mergePerson(source→target) in ONE tx: advisory lock both, preconditions (distinct, neither merged/deleted else conflict), FULL snapshot before mutation, re-point with dedup: person_names (drop dup by normalized+type re-pointing its evidence to kept, source preferred-primary→alias when target has primary), person_events (drop exact dup incl its evidence, else move), parent_child (rewrite parent/child, ABORT self-edge, drop dup), union_partners (re-point, dedup when target already in union), evidence subject person + submission_people.matched + match_candidates (keep higher score on conflict); post-check cycle over target edges→abort; mark source merged_into+deleted_at; person_merge_history snapshot + audit. resolvePersonId one hop. POST /people/:id/merge (id=source). 6 integration = full acceptance (fixture re-points name-alias/event/edge/union-partner + B merged+deleted+search-excluded + history snapshot; self-edge abort+atomic; cycle abort+atomic; double-merge conflict; self-merge invalid; resolvePersonId). Fixed test (insertPerson returns row not id). 45 API unit + 88 integration green; build clean. |
| 26 | admin-people-browser | done | 2026-07-20 | Admin canonical workbench. API (additive, no schema change): aggregate enriched with person `label`, relationship `counterpartId/counterpartLabel`, union `partners[{id,label}]`, and `mergeHistory` (all resolved in one batched query — no N+1); searchPeople now returns `birthYear/deathYear` (batched); new `GET /people/:id/evidence` aggregates evidence across person+names+events joined to source metadata (sources/service.listPersonEvidence). BFF: thin adminProxy routes for people (GET list/POST create, GET/PATCH [id], GET evidence, POST merge), relationships (POST + PATCH/DELETE [id]), unions (POST, PATCH/DELETE [id], partners add/remove), sources (GET/POST), evidence (POST); adminProxy now returns bodiless 204; people/[id] GET custom handler surfaces the merged 409 target to the browser (OracleError gained a NON-ENUMERABLE `data` field so upstream body never leaks via JSON.stringify — guarded by existing no-leak test). Admin client gained patch/del. UI (BG): /admin/people (debounced variant-aware search, живо/поверителност client filters, lifespan, status badges, „Нов човек" dialog) and /admin/people/[id] (header card with immediate privacy/living PATCH + notes; tabs Имена/Събития[honest dates]/Връзки[verification badges + потвърди/оспори/изтрий + PersonPicker add-forms]/Съюзи[create+add/remove partner]/Източници[evidence grouped by assertion, stance icons]/История[merge entries]; merge dialog: target PersonPicker → side-by-side comparison → required reason + confirm checkbox → destructive). Reusable PersonPicker exported for Task 27. 5 web tests (list, person tabs, confirm PATCH wiring, 422 cycle→BG toast, merge gating) + 2 API integration (counterpart labels + label + mergeHistory; per-person evidence across names/events + search years). OpenAPI: added /people/{id}/evidence. VERIFIED: web typecheck/lint/89 tests/build; API typecheck/lint/65 unit/103 integration/bundle; redocly valid. |
| 27 | admin-review-workspace | done | 2026-07-20 | The moderation heart (idea.md §7/§10/§8). API: suggested-relationships (maps submission_relationships via resolved matched_person_ids → parent_child [parent/child orientation from parent/child type], union [partner], sibling→sibling_hint (never an edge), other→skipped; status ready/missing_person[lists unresolved keys]/already_exists[checks current edges/shared union]); complete (guard: in_review + zero pending people → processed+processed_at, else 409 unresolved_people); defer/ignore submission-people (resolution_status + audit, in_review guard); confirm-relationship (creates CONFIRMED parent_child edge or union via existing services + attaches questionnaire evidence, cycle/conflict surfaced). BFF: 8 proxied routes (find-matches, create-person, link-person, defer, ignore, suggested-relationships, complete, confirm-relationship). UI on /admin/submissions/[id]: Tabs Общ преглед / Резолюция / Връзки + header „Маркирай като обработена" (disabled until API guard passes, 409 surfaces as BG toast). Резолюция: one card per person (staging summary + resolution badge) with the four §10 actions — „Намери съвпадения" (ranked candidates: score bar + BG reason chips + link + Свържи), „Създай нов човек" (confirm dialog), „Свържи със съществуващ" (PersonPicker), „Остави за по-късно", „Игнорирай" (+reason). Връзки: suggested list — ready→Потвърди/Пропусни, missing_person explains unresolved key, already_exists shown ✓, siblings render as „общи родители" hints. Reused Task-26 PersonPicker. 6 web tests (create-person, find→candidates→link, confirm parent_child, missing_person explain, complete-guard disabled/enabled) + 1 API integration covering the full matrix + guard + defer/ignore + confirm(edge confirmed + evidence + already_exists). OpenAPI: 5 new paths + OkResult/OptionalReasonRequest/SuggestionEndpoint. VERIFIED: web typecheck/lint/95 tests/build; API typecheck/lint/65 unit/104 integration/bundle; redocly valid. |
| 28 | genealogy-queries-resolver | done | 2026-07-20 | queries.ts recursive CTEs over CONFIRMED edges excluding merged/deleted: ancestors/descendants (min depth), commonAncestors (A+B at depth0 + ancestors, sorted by combined depth). kinship-labels.ts classifyKinship(dA,dB)→BG: direct line родител/баба-дядо/пра...баба-дядо + дете/внук/правнук, брат-сестра, чичо-леля↔племенник, cousins degree=min-1 ordinal братовчеди + removal отместени; pra() prefixes both slash sides. resolver.ts: buildGraph (parent/child/partner adjacency), BFS shortest path, blood label via closest common ancestor (confidence 100 confirmed), partner-hop→'роднина по сватовство' (through-marriage kept distinct), disconnected→connected:false, unknown→422. GET /relationship-path (admin; public redaction task-30). 5 unit (label matrix) + 5 integration (5-gen fixture: parent/child/grandparent/sibling/uncle-nephew/1st+2nd cousins/1st-once-removed, through-marriage partner-of-sibling, disconnected, unknown, confidence 100). Fixed pra() to prefix both слаш parts. 50 API unit + 93 integration green; build clean. |
| 29 | tree-projection-api | done | 2026-07-20 | DoD 10+11. projection.ts buildTreeProjection: merged-root→target (1 hop), deleted→undefined→404. generations root0/ancestors-d/descendants+d (§14, closest-to-root kept), includeSiblings (other confirmed children of root's parents at gen0), includePartners (partners share gen, synthetic union nodes at min partner gen). Nodes: person (label=preferred name, birth/death year_from, living, privacyLevel, sourceCount) + union (unionType). Edges: person→union partner, child edges confirmed+proposed carrying verificationStatus/relationshipType — routed union→child when edge has that family_union_id, else parent→child. DEDUP: one node per person id (pedigree collapse). Cap 400 nodes→truncated (keep closest to root). GET /tree/:personId + /ancestors + /descendants (params ancestors/descendants/includePartners/includeSiblings). 4 integration (generations+union-routing+dedup+partner edges, pedigree collapse single node, merged-root resolution+deleted 404, truncation cap). 50 API unit + 97 integration green; build clean. |
| 30 | privacy-redaction | done | 2026-07-20 | DoD 12. privacy/redact.ts redactTreeNode(node,view): admin=unchanged, union=passthrough; public: living→'Жив член на семейството'+null years (never identifiable even if flagged public — documented policy), private deceased→'Член на семейството'+masked, family/public deceased→name+years+settlement+sourceCount. birthDecade '1980-те'. FORBIDDEN_PUBLIC_FIELDS constant. Applied in buildTreeProjection (view param). Tree + relationship-path routes NO LONGER requireRole — resolveView forces public unless actor is admin AND view!=public; relationship-path public returns only label+confidence (no ids/path/commonAncestors). BFF public routes /api/tree/[personId] + /api/relationship (actor public, view forced public). 6 unit (redaction matrix incl. living-flagged-public masked) + 3 integration (public masks living+private + DEEP-SCAN no forbidden field/notes leak + admin sees real names, public path label-only no-ids, non-admin can't force admin view). 56 API unit + 100 integration + 84 web green; builds clean. |
| 31 | tree-visualization | done | 2026-07-20 | DoD §14. Deps: @xyflow/react 12.11 + elkjs 0.12. features/tree: projection-to-flow.ts (PURE projectionToFlow → RF person/union nodes [dedup already done API-side] + edges; toFlowEdge dashes `proposed` edges + „предложена" label, labels adoptive/step; toElkGraph forces one ELK partition per generation [gen−minGen] so ancestors sit above root; applyElkPositions). layout.ts + elk.worker.ts (ELK `layered` DOWN in a Web Worker via `new Worker(new URL('./elk.worker.ts',...))`, 15s timeout, graceful fallback to main-thread elk.bundled). PersonNode (avatar initials, name/mask label, honest years, verification + source chips, root ring, subdued masked style) + UnionNode (♥ connector, type tooltip). TreeCanvas (ReactFlowProvider, nodeTypes, Controls/Background, „Побери"/„Центрирай" fitView, node→info popover, layout-loading overlay keyed by base graph so no stale render / no sync setState). TreeExplorer (URL ?root= shareable; admin→PersonPicker, public→PublicRootSearch; TanStack query public /api/tree/[id] vs admin BFF; loading/empty[„Няма данни за показване"]/error+retry; truncated badge). Pages: /tree (public, Suspense) + /admin/tree. BFF: /api/admin/tree/[personId] (view=admin), /api/tree/search (public). API: publicSearchPeople + GET /people/public-search (NO requireRole, registered before /people/:id; returns ONLY deceased family/public — living never findable, private masked; min 2 chars). 5 web tests (projectionToFlow nodes/root/edges, proposed dash+label, ELK partition-per-gen + union wiring, PersonNode full+masked via ReactFlowProvider) + 2 API integration (public-search excludes living+private, min-length). OpenAPI: /people/public-search. docs/tree-fixture-bg.md (seed steps). VERIFIED: web typecheck/lint/100 tests/build (worker+RF bundled, both pages generated); API typecheck/lint/65 unit/106 integration/bundle; redocly valid. |
| 32 | tree-ux-path-highlight | done | 2026-07-20 | DoD §14 interaction set (web-only; +1 admin relationship BFF route). Pure/tested: graph-ops.ts (mergeProjections dedupe by node/edge id keeping root; childrenOf/parentsOf with union pass-through; computeHidden subtree + orphaned-union hide; visibleProjection; collapsibleIds), view-mode.ts (viewModeParams: ancestors→desc0, descendants→anc0, combined→anc/desc−1; projectionQuery), highlight.ts (computeHighlight: path persons + between-unions + connecting edges; pathReducer start→pick A→pick B→result→clear, ignores dup/inactive). UI: TreeExplorer orchestrates root(URL ?root=)/viewMode/depth; base query + per-key extra[] merged via mergeProjections (load-more anchored at boundary node), collapsedByKey (no effect resets — keyed by root|mode|depth); Предци/Потомци/Комбиниран toggle; „Покажи още" depth+1; „Връзка между двама" mode (pick 2 nodes → admin /api/admin/relationship [full path] or public /api/relationship [label only] → computeHighlight dims rest + result bar with label/confidence/common-ancestor count). TreeCanvas overlays highlight-dim + collapse chevron + boundary „+" load-more onto ELK-laid nodes WITHOUT re-layout (memoized), double-click→re-root. PersonNode chevron/load-more buttons + dim/highlight styling. SidePanel (select → details + „Направи корен"/„Виж връзката с…"/admin „Отвори в администрацията"). MobilePedigree (<768px via useMediaQuery/useSyncExternalStore — no sync setState — root card + Родители/Деца tap-to-reroot, no h-scroll). Node-count banner >300. BFF: /api/admin/relationship (view=admin, full path). 8 web unit tests (mergeProjections, collapse childrenOf/hidden/collapsible, viewModeParams matrix, computeHighlight, pathReducer walk+guard). VERIFIED: web typecheck/lint/108 tests/build (both tree pages generated). No API/OpenAPI change (reuses relationship-path). |
| 33 | backup-restore | done | 2026-07-20 | DoD 19/§21. scripts/backup-db.sh (daily cron: flock single-instance, pg_dump --format=custom via docker compose exec — password never logged, sha256, age encrypt to AGE_RECIPIENT [+optional AGE_VERIFY_RECIPIENT], refuses unencrypted upload, manifest.json, rclone upload AFTER successful dump, weekly[Sun]/monthly[1st] promotion, retention prune 14/8/12 via lsf|sort|head -n -N, backup-status.json feeds backup_last_success_timestamp gauge, trap cleanup, notify webhook). restore-db.sh (rclone download→sha256 verify→age -d with offline AGE_IDENTITY_FILE→pg_restore --clean --if-exists; refuses prod DB without --force). verify-backup.sh (monthly: FULL restore test into temp DB when AGE_VERIFY_IDENTITY_FILE present [migrations table + people count + integrity probes: no self-parent, no merged-as-parent, drops temp DB] else LIGHTWEIGHT download+age-header check; status+webhook on fail; resolves restore-db.sh via SCRIPT_DIR not cwd). infra/oracle/cron.d/familytree (backup 03:30, R2 copy Sun 04:00, verify 1st 05:00, check-disk 06:00; sources .env). docs/backup-and-restore-bg.md (BG runbook: dual-key design keeps offline key off VM, rotation, restore, corruption playbook). env.example: AGE_RECIPIENT/AGE_VERIFY_*/RCLONE_REMOTE_*/BACKUP_DIR/RETAIN_*/NOTIFY_WEBHOOK_URL. Server-targeted (age/rclone/flock are Linux tools) — verified bash -n on all three; shellcheck not installed locally. |
| 34 | exports-gedcom-json-csv | done | 2026-07-20 | DoD 1.12. export/data.ts fetchExportData (canonical graph excl merged/deleted, stable id order, publicOnly drops living + filters their rows). formats.ts: toGedcom (5.5.1: HEAD/CHAR UTF-8, INDI + NAME /surname/, BIRT/DEAT with gedcomDate honoring precision exact→DD MON YYYY/month/year/approximate→ABT/range→BET AND/unknown→omit NEVER fake, FAM per union + synthetic for unionless pairs with HUSB/WIFE/CHIL, SOUR records), toJsonExport (flat versioned envelope not nested), toCsvPeople/Relationships (UTF-8 BOM for Excel Cyrillic). CLI src/export.ts (node dist/export.js gedcom|json|csv-people|csv-relationships --out --public), esbuild bundles dist/export.js, npm script + scripts/export-gedcom.sh wrapper. 6 unit (gedcomDate precision matrix, GEDCOM structural validity/slashed-surname/determinism, JSON envelope, CSV BOM) + 1 integration (public drops living + GEDCOM excludes living name). Bundled CLI smoke-tested against dev DB. Also removed a stray unused import that slipped into task-30. 62 API unit + 101 integration green; build clean. |
| 35 | deploy-workflows | done | 2026-07-20 | DoD 17/18. scripts/deploy.sh (SSH remote: pull→migrate FIRST→up→health gate 10×3s→roll forward on healthy else exit1+print rollback cmd; prev tag in .deploy-tag). deploy-api.yml (push main api/shared/infra paths + dispatch image_tag; jobs test→build-push ARM64 GHCR commit-SHA+:main never latest-only→deploy scp+ssh health gate; gated on DEPLOY_ENABLED var→safe skip; needs DEPLOY_SSH_HOST/USER/KEY secrets + API_DOMAIN var). deploy-web.yml (Vercel native git integration deploys; workflow only gates lint/typecheck/test/build). deployment-oracle-bg.md finalized (secrets/vars, deploy flow, rollback, Vercel setup). VERIFIED: actionlint clean both workflows, shellcheck clean deploy.sh+check-disk+export-gedcom, bash -n OK. No secrets in repo; deploy no-op without DEPLOY_ENABLED. |
| 36 | monitoring | done | 2026-07-20 | metrics/registry.ts (prom-client): /metrics endpoint (Caddy returns 404→internal-only), http_requests_total + http_request_duration_seconds labelled by ROUTE PATTERN (c.req.routePath, bounded cardinality) not raw path, db_pool_* gauges (collect on scrape), business counters submissions_created/hmac_failures/turnstile_rejections/rate_limit_hits/spam_flagged, backup_last_success_timestamp (reads backup-status.json via module path var). Counters wired: createSubmission (created/rate_limit/spam), hmac middleware (failures), abuse-events endpoint (turnstile/rate/spam). POST /v1/internal/abuse-events (BFF fires, audits, no payload). BFF reportAbuse fire-and-forget on turnstile reject. scripts/check-disk.sh (df threshold→webhook+exit1, DF_OUTPUT injectable). Request logs already metadata-only + X-Request-Id correlation both sides. Monitoring section in architecture.md. 3 unit (metrics text+counters registered, http counter by route, hmac_failures increments) + disk script tested (90%→1, 50%→0). 65 API unit + 101 integration + web build green. |
| 37 | e2e-playwright | done | 2026-07-20 | DoD §23 E2E infrastructure + suite. docker-compose.e2e.yml (postgres :5544 + API built from source + one-shot migrate; isolated DB familytree_e2e, own HMAC secret, API on 127.0.0.1:8787). Auth.js Credentials provider e2e-credentials.ts wired ONLY when E2E_TEST_MODE=1 via buildProviders(); HARD GUARD assertE2EAllowed() throws if VERCEL_ENV/APP_ENV=production (fails closed) — documented in security.md. playwright.config.ts (apps/web, e2e/ dir, own .env.e2e loader, webServer=next dev, chromium+mobile projects, trace retain-on-failure, workers=1). Specs (§23 steps 1-8): questionnaire (fill BG form→Turnstile test key→submit→assert pending via admin API), admin-review (login→start-review→create SELF+FATHER→link MOTHER via picker→confirm edges→mark processed), tree (§23.7 admin tree shows parents branch), public-privacy (§23.8 public tree masks living „Жив член на семейството" + public search can't find living), invite (max-1 consumed then rejected). helpers.ts (Auth.js credentials login via csrf+callback, admin BFF get/post, payload builder, submit). scripts/e2e-up.sh (build→migrate→up→health-gate) + e2e-reset-db.sh/.sql (TRUNCATE staging+canonical CASCADE, keep migrations). .env.e2e.example (official Turnstile test keys 1x000…AA). Nightly workflow e2e.yml (schedule+dispatch, NOT PR-blocking, traces on failure, teardown). docs/e2e-testing.md (run steps + Windows notes). VERIFIED STATICALLY: e2e-credentials guard 6/6 unit tests, web typecheck/lint/114 tests/build (specs typecheck-clean), actionlint clean (docker), bash -n, docker compose config valid. NOTE: full browser run needs Docker+Playwright browsers+running stack (via e2e-up.sh + npm run e2e / nightly CI) — not executed headlessly in this authoring env; selectors authored against the real BG labels. |
| 38 | file-uploads (post-MVP, optional) | done | 2026-07-20 | idea.md §24 Phase 6/§15. Migration 0003_files (person_id?/source_id? SET NULL, content_type CHECK allowlist, size>0, storage_key UNIQUE server-generated, subject-present CHECK, soft-delete; partial indexes). files/: sniff.ts (magic-byte content-type detection — JPEG/PNG/WEBP/PDF, exe→null, pure/tested), strip.ts (PURE-JS metadata removal — JPEG APPn/COM, PNG text/eXIf chunks, WebP EXIF/XMP — no native dep, keeps pixels), storage.ts (FileStorage interface + LocalDirStorage, traversal-guarded, get/setFileStorage test seam), repo.ts, service.ts (uploadFile: no-subject/size/sniff→10MB cap→strip→sha256→object-first-then-row w/ orphan cleanup + audit; getFileContent; deleteFile soft+object rm). Routes (admin-only, JSON+base64 contract for HMAC): POST /files (413/422/422/422 mapped), GET /files?personId/sourceId (meta), GET /files/:id (base64 content — NEVER public), DELETE (204). base64 cap = DoS ceiling (2x) so normal oversize→413 not 400. BFF: /api/admin/files (GET list via proxy; POST custom multipart→base64→signed API, 10MB guard), /api/admin/files/[id] (GET streams decoded bytes w/ content-disposition + private no-store; DELETE proxy). UI: FilesTab on person page („Файлове" tab — dropzone drag/drop + picker, image thumbnails/PDF icon, download, delete-with-confirm, upload progress, BG errors). Backup: backup-db.sh syncs FILE_STORAGE_DIR (rclone sync) + file count/bytes in manifest. prod compose: writable bind-mount ./file-storage→/app/file-storage (writable despite read_only). ADR 0005 (proxy-over-presigned + local-dir + pure-JS strip rationale). OpenAPI: 4 file endpoints + FileMeta + files tag. env.example + architecture.md §8.1. Chose pure-JS strip over sharp to keep the dist-only image + avoid ARM64 native risk. 5 API unit (sniff matrix + JPEG-EXIF/PNG-text strip) + 3 API integration (upload→strip-verified→fetch→delete, exe-renamed 422, oversize 413) + 2 web (thumbnails/pdf, delete). VERIFIED: web typecheck/lint/116 tests/build; API typecheck/lint/70 unit/109 integration/bundle; redocly valid; backup bash -n. |
